name: Update HAYQ Bot

on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * *' # ստուգում է GitHub repo–ում նոր commit–երը 10 րոպեն մեկ

jobs:
  update-hayq-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          npm install

      - name: Start bot (debug mode)
        run: |
          echo "🚀 Starting HAYQ Bot..."
          node bot.js
